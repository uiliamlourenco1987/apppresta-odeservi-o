# Banco de dados (SQLite no desenvolvimento)
DATABASE_URL="file:./dev.db"

# Chave secreta para assinar a sessão (troque por um valor aleatório longo em produção)
AUTH_SECRET="troque-por-uma-chave-secreta-bem-longa-e-aleatoria"

# (Opcional) Envio de notificações. Sem isso, as notificações ficam como "SIMULADO".
# Aponte para um gateway (Zapier / n8n / API de e-mail ou WhatsApp) que receberá
# um POST JSON com { canal, destinatario, assunto, corpo }.
# NOTIFICACAO_WEBHOOK_URL="https://seu-gateway.exemplo.com/webhook"
